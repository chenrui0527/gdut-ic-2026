# 大一招新考核作品 · 陈睿

本仓库是集成电路协会大一招新考核的提交作品，包含个人简介、个人网站、小游戏，以及完成过程中的记录。

## 仓库结构

```
.
├── profile/个人简介.pdf      # 个人简介（单页 PDF）
├── website/                  # 个人网站（纯 HTML/CSS，部署在 GitHub Pages）
│   ├── index.html
│   └── style.css
├── game/snake.html           # 小游戏：贪吃蛇（人工 + AI 自动演示，单文件）
├── LICENSE                   # MIT 协议
└── README.md
```

仓库地址：https://github.com/chenrui0527/gdut-ic-2026

个人网站地址：https://chenrui0527.github.io/gdut-ic-2026/website/

---

## 一、环境准备

| 项目 | 情况 |
|---|---|
| GitHub 账号与仓库 | 已注册；作品全部提交到本仓库 |
| AI 编程工具 | **DeepSeek Harness**（自装）与 **Codex**，实际用于本项目的开发与查错 |
| Git | Git 2.53（Windows） |
| 代码编辑器 | VS Code |

---

## 二、个人简介

1. **PDF**：`profile/个人简介.pdf`，内容包含姓名、专业、兴趣方向、技能、联系方式。
2. **个人网站**：`website/` 下的 `index.html` + `style.css`，手写 HTML/CSS，无框架。
   部署方式：仓库 **Settings → Pages → Source 选 `Deploy from a branch` → 分支 `main`、目录 `/ (root)`**，
   保存后访问 `https://<用户名>.github.io/<仓库名>/website/`。

---

## 三、通用素养

### 1. Git 操作

- 本仓库的提交次数 ≥ 3 次，每一步的改动都单独提交，可以看出是逐步做出来的。
- **什么是分支（branch）**：分支是从某次提交上分出来的一条独立开发线，你可以在这条线上改代码而不影响主线。
- **什么是合并（merge）**：合并就是把一条分支上的改动并回另一条分支（通常是 main），让主线获得这些新改动。

### 2. 命令行

| 命令 | 用途 |
|---|---|
| `cd 目录` | 切换当前所在目录（`cd ..` 回上一级） |
| `ls`（Windows 下 `dir`） | 列出当前目录里的文件和文件夹 |
| `mkdir 名字` | 新建文件夹 |
| `git status` | 看当前有哪些文件被改动、哪些已暂存 |
| `git add .` | 把改动放进暂存区，准备提交 |
| `git commit -m "说明"` | 把暂存区的改动记录成一次提交 |
| `git push` | 把本地提交推送到 GitHub |
| `git log --oneline` | 查看历史提交列表 |
| `git branch` / `git checkout -b 名字` | 查看分支 / 新建并切换到新分支 |
| `git merge 分支名` | 把某个分支合并到当前分支 |

### 3. 信息与安全意识

**LICENSE**：本项目使用 **MIT 协议**。选择它的原因：MIT 是最简单、限制最少的开源协议之一，允许别人自由使用、修改、分发我的代码，只要保留版权声明；对个人作品来说，既方便别人学习和复用，也避免了很多法律上的麻烦。（同时我会在仓库设置里开启 GitHub 两步验证，见文末清单。）

**一条“AI 给过、但我查证后认为不对”的信息**：

> AI 最初给我的贪吃蛇 AI 方案是“每一步都沿最短路直冲食物”，并说这样就能稳定吃到 15 个以上。
> 为了验证这句话，这个项目把游戏里的核心逻辑抽出来写了无头测试脚本，让 AI 自动玩 100 局做统计：
> **结果 100 局里有 15 局在吃到 8～14 个食物时就把自己困死了（最少只吃到 8 个）**，
> 达不到“连续 15 个不死”的硬指标。
> 后来把策略改成“走之前先检查走完这一步还剩多少活动空间”，再跑 100 局：
> **100 局全部 ≥15 个，最少 18 个，中位数 58 个**。所以 AI 给的结论要用数据验证。

### 4. 防“纯粘贴”承诺：两处 AI 生成代码的关键逻辑，我用自己的话说明

**第一处：AI 自动玩为什么能“不把自己困死”**

AI 每一步做两件事：先用广搜（BFS）算出到食物的最短路的**第一步方向**；但在真正走之前，它会先“模拟走一步”，然后从这个新蛇头出发做一次**洪水填充**，数一数还能到达多少空格。如果可到达的空格数**大于蛇的当前长度**，就认为这一步是安全的，可以走；否则就放弃吃食物，改成在所有能走的方向里挑“活动空间最大”的那一步（效果上相当于先去追自己的尾巴），把身体重新拉开，等有了空间再去吃。所以它不是“看见食物就冲”，而是**每一步都先问一句：走完这步，我还回得来吗**。

**第二处：计分统计为什么要那样写**

考核要求“新一局不清零”。我的做法是把**最高分和累计局数**存成一个对象，写进浏览器的 `localStorage`：每局结束只更新这个对象，只有点“重置统计”才清零；而**当前得分**是每局重新开始的。这样“新一局不清零”指的是统计不清零，不会出现“上一局的分算到这一局头上”的混乱。主题（深色/浅色）也一起存在 `localStorage` 里，所以刷新页面后还是上次选的那套配色。

---

## 四、小游戏：贪吃蛇（人工 + AI 自动演示）

**运行方式**：直接双击 `game/snake.html`，用浏览器打开即可，不需要安装任何环境、不依赖框架，全部代码在一个文件里。

### 阶段一：能自己玩的版本

- **操作**：方向键或 WASD 控制，禁止 180° 掉头（用 `d[0]+h[0]===0 && d[1]+h[1]===0` 判断方向是否相反）。
- **规则**：吃到食物加 10 分、蛇身加长；撞墙或撞到自己游戏结束；棋盘 15×15。
- **界面状态**：右侧面板实时显示当前得分、蛇长、本局已吃、最高分、累计局数。
- **怎么验证**：打开页面用方向键试玩，确认按键响应正常、吃食物与加分同步、撞墙或撞到自己会正确结束并弹出结算。

### 阶段二：AI 自动演示

点“AI 自动演示”后，AI 接管操作，面板上的 **“AI 连续吃（目标 15）”** 会实时计数。

**AI 策略（按顺序判断）**：
1. 先用 BFS 算出到食物的最短路，但在走之前先看一眼“走完这一步还剩多少活动空间”；
2. 这条“安全线”跟着蛇的长度变：蛇越长，要求保留的空间越多（长度 + 至少 30% 的余量）；当蛇已经占掉棋盘四分之一以上时，不允许贴着墙去吃（贴墙容易把自己逼进角落）；
3. 不能安全直冲时，在所有合法走法里挑一步综合分最高的：活动空间最大、尽量靠棋盘中间、离食物更近。

**验收数据（不是估计值，是脚本实测）**：把 `snake.html` 里的核心逻辑抽出来，用一个 Node 无头测试脚本让 AI 在 15×15 棋盘上自动玩 **100 局**（每局随机食物序列）：

| 策略 | 100 局中最少吃到 | 中位数 | 达到 15 个的比例 |
|---|---|---|---|
| 只按最短路直冲食物（最初方案） | 8 个 | 22 个 | 85 / 100 |
| 加入“走前检查剩余空间”（最终方案） | **18 个** | 58 个 | **100 / 100** |

最终方案 100 局全部满足“连续吃完 15 个食物不死”的硬指标。

后来又按需求把 AI 从“只判断剩余空间”升级成“结合自己的长度和位置”，也就是现在线上跑的这版。用一个更快的测试（60 局、吃到 20 个就停）对比三代策略：

| 策略 | 60 局中最少吃到 | 达到 15 个的比例 | 说明 |
|---|---|---|---|
| 只按最短路直冲食物 | 8 个 | 52 / 60 | 约 13% 的局会提前把自己困死 |
| 加“走前检查剩余空间” | 18 个 | 60 / 60 | 没有提前死亡 |
| 结合长度与位置（当前版本） | 20 个（吃满上限才停） | 60 / 60 | 60 局全部吃满 20 个才停，一次都没提前死 |

（这个快测把“吃到 20 个”设为停止条件，所以“最少 20 个”表示它没有在达到上限前死亡。）

### 新增功能（考核要求至少 1 个，本项目实现了 2 个）

1. **计分制**：记录当前得分、最高分、累计局数；“新一局”只重置本局得分，统计继续累加；另有“重置统计”按钮专门清零。
2. **主题切换**：深色 / 浅色两套配色，一键即时切换，选择结果写入 `localStorage`，刷新后保留。

### 给 AI 的关键提示词与迭代过程

下表是本项目的开发与迭代记录：每一轮都是先提出要求，看结果，再决定下一步怎么改。带引号的部分是原话。

| 轮次 | 提出的要求（要点） | 结果 | 调整方式 |
|---|---|---|---|
| 1 | “写一个纯前端单文件贪吃蛇，能键盘玩，有得分和结束判定” | 能玩，但没有自动演示 | 补需求：加 AI 自动玩 |
| 2 | “一开始只让 AI 走离食物最近的那一步” | AI 会把自己困死：100 局里有 15 局吃不到 15 个（最少只吃到 8 个） | 先写个测试脚本跑统计，看清楚它是怎么死的 |
| 3 | “先用 BFS 算出到食物的最短路，但走之前先看一眼走完这一步还剩多少活动空间” | 100 局全部 ≥15（最少 18 个，中位数 58 个） | 觉得还不够，要求它把自己的情况也考虑进去 |
| 4 | “加上最高分/局数统计，新一局不清零，再加主题切换” | 功能完成 | 手动测试刷新后主题是否保留 |
| 5 | “把棋盘从 20×20 改成 15×15，AI 也要能正常跑”（我提的） | 棋盘改为 15×15，重跑 100 局统计：AI 依然 100/100 达标（最少 18 个，中位数 58 个） | 打开 `game/snake.html` 点“AI 自动演示”，可以看到 15×15 棋盘上 AI 正常运行 |
| 6 | “先用 BFS 算出到食物的最短路，但走之前先看一眼走完这一步还剩多少活动空间：<br>1. 这条“安全线”跟着蛇的长度变——蛇越长，要求保留的空间越多（长度 + 至少 30% 的余量）；<br>2. 位置也算进去：当蛇已经占掉棋盘四分之一以上时，不允许贴着墙去吃，因为贴墙容易把自己逼进死角；<br>3. 不能安全直冲时，就在所有能走的方向里挑综合分最高的一步：空间最大、尽量靠棋盘中间、离食物更近” | 改成这版之后，60 局快测全部吃满 20 个（上限）才停，一次都没有提前死亡 | 打开 `game/snake.html` 点“AI 自动演示”，看它避开边角、把空间留给自己 |

---

## 五、进阶挑战：复现 FlowTune 论文（多臂老虎机做逻辑综合流程搜索）

进度挑战选的是“读懂论文 + 跑通前端后端”这一类。论文：**FlowTune: End-to-End Automatic Logic Optimization Exploration via Domain-Specific Multiarmed Bandit**（IEEE TCAD，作者 Cunxi Yu），<https://ieeexplore.ieee.org/abstract/document/9916059>。

- **前端（设计输入与功能验证）**：写 RTL（4 位计数器、8×8 乘法器），用 Yosys 读入并展开，再用**形式化等价性检查**证明综合出来的门级网表与原始 RTL 功能一致，跑完输出 `Equivalence successfully proven!`。
- **后端（逻辑综合与优化）**：把 RTL 综合成门级网表，统计门数与关键路径深度。
- **论文核心（多臂老虎机）**：把流程优化建模成**多阶段多臂老虎机**，用 UCB1 在 3 个阶段的候选配方里选择（共 54 种组合），把真实综合出来的门数当奖励反馈。
- **实测结果**：8×8 乘法器门数 769 → **367**（−52%），4 位计数器 39 → **10**（−74%）；老虎机只用了 **2 次**评估就找到了穷举 **54 次**才能确认的最优配方。

代码、运行方式和更详细的说明见 [flowtune/README.md](flowtune/README.md)（`flowtune/` 目录）。

---

## 关于 AI 生成代码的说明（诚实标注）

- **代码部分（游戏、网站、PDF 生成脚本）由 AI 工具生成**，需求来自考核任务书，我用 Codex / DeepSeek Harness 提出需求并让它实现；
- 项目里的验证工作（100 局自动玩统计、网页语法检查）由配套脚本完成，测试脚本和游戏共用同一份核心逻辑，所以测的就是实际跑的那份代码；
- **我负责的部分**：注册并管理 GitHub 仓库、开启两步验证、部署个人网站（GitHub Pages）、确认作品能正常打开后提交。
